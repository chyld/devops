Prefs - trackpad, sound, bluetooth, dock, finder
Amphetamine
Chrome
Google Account - Clear History
CloudApp
SizeUp
MacVim
Docker
Get Wallpaper from Google Drive

mkdir Code Data Temp

VSCode
  - plugins
  - fonts
  - get Dank Mono - delete .ttf font (dups)
  - config

Hyper - get config from github
      - git + gitconfig
      - zsh and reboot
      - zsh plugins
      - https://github.com/powerline/fonts
      - node
      - miniconda python (readonly root fs)
      - chmod -R 500 miniconda3
      - conda create -n py37 python=3.7
      - conda install numpy pandas jupyter ipython
      - pip install asciinema httpie
      - vim plug
      - fzf
      - install homebrew
      - brew install tree htop wget the_silver_searcher

